{% macro content(spec) %}

{% set spec = spec or {} %}


<nav class="{{spec.class}}">
  <p>{{spec.texto}}</p>
   {% if spec.super %}
    ESTO ES SUPER!

  {% endif %}

  <p>This is an example paragraph for the nav module</p>
</div>
{% endmacro %}
